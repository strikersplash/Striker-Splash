# Production Security Settings
NODE_ENV=production
ENABLE_DEBUG_LOGGING=false
ENABLE_SENSITIVE_LOGGING=false

# Only log errors and warnings in production
LOG_LEVEL=warn

# Security headers
ENABLE_SECURITY_HEADERS=true

# Cloud Deployment Configuration (SSL handled by cloud provider)
PORT=3000
ENABLE_HTTPS=false
# ↑ Cloud load balancer handles HTTPS, app runs HTTP internally

# Trust cloud load balancer proxy
TRUST_PROXY=true

# Session security
SESSION_SECRET=your_super_secure_production_session_secret_change_this_before_deployment

# Production Security
SESSION_SECRET=your_production_session_secret_generate_a_strong_one
ENABLE_DEBUG_LOGGING=false
ENABLE_SENSITIVE_LOGGING=false
LOG_LEVEL=warn
ENABLE_SECURITY_HEADERS=true
